[project]
name = "rag-agent-learning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ysyq", email = "NguyetAlves@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "dashscope>=1.27.1",
    "langchain>=1.3.18",
    "langchain-classic>=1.0.8",
    "langchain-community>=0.4.2",
    "langchain-core>=1.6.0",
    "langchain-openai>=1.6.0",
]

[project.scripts]
rag-agent-learning = "rag_agent_learning:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
